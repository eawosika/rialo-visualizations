{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--config",
        ".claude/playwright-mcp.config.json",
        "--headless"
      ]
    }
  }
}